## 1. Resolver and config persistence

- [x] 1.1 Implement "Claude target instructions file is resolved per repo", "Resolved instructions file is persisted in `.aspens.json`", and "Config inference records AGENTS.md for AGENTS.md-only claude repos": in `src/lib/target.js`, add `CLAUDE_INSTRUCTIONS_FILES = ['CLAUDE.md', 'AGENTS.md']`, `isAgentsMdShim(content)`, `detectClaudeInstructionsFile(repoPath)`, and `resolveClaudeTarget(repoPath, { instructionsFile } = {})` returning `{ ...TARGETS.claude, instructionsFile }` with precedence override > `.aspens.json` > detection > `CLAUDE.md`; extend `isValidConfig`/`writeConfig` to accept and preserve an optional `instructionsFile`; make `inferConfig` set `instructionsFile: 'AGENTS.md'` for the AGENTS.md-only claude case.
- [ ] 1.2 Add `tests/claude-instructions-file.test.js` covering every scenario in `specs/claude-instructions-file-resolution/spec.md`: shim detection (blank lines, HTML comments, `@./AGENTS.md`, extra content), precedence, invalid override throws, config validation/preservation/corrupt fallback, and `inferConfig` recovery.

## 2. CLI option and doc-init output path

- [x] 2.1 Implement "`aspens doc init` accepts `--instructions-file`": in `bin/cli.js`, add `--instructions-file <name>` to `doc init` (help text: `Root instructions file for the claude target: CLAUDE.md or AGENTS.md`) and pass it through to `docInitCommand` options.
- [ ] 2.2 Implement "doc init generates the root instructions file under the resolved name" and the doc-init half of "An `@AGENTS.md` shim in CLAUDE.md is preserved byte-for-byte": in `src/commands/doc-init.js`, validate the option (CliError on invalid value or non-claude target before any LLM call), call `resolveClaudeTarget` once at startup and store it as the primary claude target, derive `CANONICAL_VARS.instructionsFile`, `_allowedPaths`, `instructionsArtifactLabel`, the `parseLLMOutput` expected path, the retry prompt text, the `claudeMdExists` strategy check, and the `ensureAspensImportBlock` write site from it, and pass `instructionsFile` to `writeConfig` at completion.
- [ ] 2.3 In `src/prompts/doc-init-claudemd.md`, replace remaining literal `CLAUDE.md` output-path references with the `{{instructionsFile}}` variable already supplied via `CANONICAL_VARS`.
- [ ] 2.4 Add `tests/doc-init-instructions-file.test.js` (mocked runner, following `tests/doc-init-reuse-source.test.js`) covering: override forces `AGENTS.md` on a fresh repo with no `CLAUDE.md` created; shim `CLAUDE.md` is byte-identical after a run and the aspens block lands in `AGENTS.md`; `<file path="CLAUDE.md">` output is rejected and retried with `AGENTS.md`; improve strategy includes `## Existing AGENTS.md`; skip strategy honors an existing `AGENTS.md`; invalid value and `--target codex` fail with CliError before any runner call; `.aspens.json` records `instructionsFile`; CLAUDE.md-resolved runs produce identical output to today.
- [ ] 2.5 Add a case to `tests/prompt-loader.test.js` asserting that loading `doc-init-claudemd` with `instructionsFile: 'AGENTS.md'` yields no `CLAUDE.md` output-path reference.

## 3. doc-sync, context, and impact consumers

- [ ] 3.1 Implement "doc sync operates on the persisted instructions file" and the doc-sync half of the shim-preservation requirement: in `src/commands/doc-sync.js`, replace each `sourceTarget?.instructionsFile || 'CLAUDE.md'` fallback and the `TARGETS.claude` source-target lookup with the target returned by `resolveClaudeTarget(repoPath)` (config-backed, never re-detecting in the hook path), so `repairDeterministicSections`, `--refresh`, the LLM sync prompt's existing-instructions section, and the pending-file lookup all use the recorded name.
- [ ] 3.2 Extend `tests/doc-sync-managed-block.test.js` with cases: recorded `AGENTS.md` + shim `CLAUDE.md` repair leaves `CLAUDE.md` unchanged and fixes `AGENTS.md`; `--refresh` refreshes `AGENTS.md` without creating `CLAUDE.md`; recorded `AGENTS.md` but file deleted makes no root change and no `CLAUDE.md`.
- [ ] 3.3 Implement the context half of "Context building and impact analysis use the resolved name": edit only `src/lib/context-builder.js` so the `instructionsFile` default comes from `resolveClaudeTarget(repoPath).instructionsFile` instead of the literal `'CLAUDE.md'`, and compute the alternative file as the other member of `CLAUDE_INSTRUCTIONS_FILES` when the primary is a claude name. No caller changes: doc-init and doc-sync already pass `instructionsFile` explicitly.
- [ ] 3.4 Add `tests/context-builder-instructions-file.test.js` asserting the `## Existing AGENTS.md` primary section followed by `## Existing CLAUDE.md` alternative for a shim repo, and the reverse ordering for a `CLAUDE.md` repo.
- [ ] 3.5 Implement the impact half of the same requirement: in `src/lib/impact.js` and `src/commands/doc-impact.js`, build the claude target via `resolveClaudeTarget(repoPath)` before `summarizeTarget` so `instructionsFile`, `instructionExists`, and `lastUpdated` reflect the recorded name.
- [ ] 3.6 Extend `tests/doc-impact.test.js` with a case where `.aspens.json` records `AGENTS.md`, `AGENTS.md` exists, and `CLAUDE.md` does not: the JSON report shows `instructionsFile: "AGENTS.md"` and `instructionExists: true`.

## 4. Guard rails and real-tool verification

- [ ] 4.1 Test-only task, no `src/` edits: add `tests/no-claude-md-literals.test.js` that greps `src/commands` and `src/lib` (excluding `src/lib/target.js`) for the quoted literal `CLAUDE.md` and fails on any hit, per "No stray `CLAUDE.md` literals outside the resolver". The literal removals themselves are done in 2.2, 3.1, 3.3, and 3.5; this task only adds the guard.
- [ ] 4.2 Update `.claude/skills/codex-support/skill.md` and `.claude/skills/skill-generation/skill.md` to document the resolver, the `--instructions-file` option, the `.aspens.json` `instructionsFile` field, and the shim-preservation rule; add the option to the `aspens doc init` line in the root `CLAUDE.md` Commands section.
- [ ] 4.3 [e2e] Manual verification in a scratch repo with a shim `CLAUDE.md` (`@AGENTS.md`) and a populated `AGENTS.md`: run `aspens doc init --target claude --yes`, then open Claude Code and confirm via `/memory` that `.claude/aspens-index.md` is loaded through the two-hop import chain; record the Claude Code version and result in the PR description.
- [ ] 4.4 [e2e] Run `npm test` and `npm run lint`; both must pass before the change is marked complete.
